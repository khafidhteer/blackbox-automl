# BlackBox AutoML - Dependencies (AutoGluon Edition)
# AutoGluon is the core ML engine — it bundles pandas, numpy, scikit-learn,
# xgboost, lightgbm, catboost, and more.
# Use autogluon.tabular for just the tabular ML engine (smaller install).
# Use autogluon for the full suite (includes multimodal, timeseries, etc.).
autogluon.tabular>=1.0.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Notebook generation
nbformat>=5.7.0
jupyter>=1.0.0
